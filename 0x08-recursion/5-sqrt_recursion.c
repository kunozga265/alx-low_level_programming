#include "main.h"

/**
 *  _sqrt_recursion - calculates the square root of number
 * @n: number
 * Return: Its power
 */
int _sqrt_recursion(int n)
{
	if (y == 1)
	{
		return (1);
	}
	else if (y < 0)
	{
		return (-1);
	}
	else
	{
		return (n);
	}
}
