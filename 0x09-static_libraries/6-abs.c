#include "main.h"

/**
 * _abs - function
 * @n: number checked
 *
 * Description: Determines absolute value
 * Return: Absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
